server:
  host: "0.0.0.0"
  listen_port: "8000"

static:
  dir: "/app/internal/html/dist"

resources:
  - name: Server1
    endpoint: /api/v1/
    destination_url: "http://go_server:8080/api/v1/"
  - name: Server2
    endpoint: /server2
    destination_url: "http://localhost:9002"
  - name: Server3
    endpoint: /server3
    destination_url: "http://localhost:9003"
