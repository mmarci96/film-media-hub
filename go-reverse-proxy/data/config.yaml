server:
  host: "0.0.0.0"
  listen_port: "8000"

static:
  dir: "/home/meirl/Documents/film-media-hub/client/dist"

resources:
  - name: Film Hub Server1
    endpoint: /api/v1/
    destination_url: "http://localhost:8080/api/v1/"
  - name: Example
    endpoint: /example
    destination_url: "http://localhost:3000"
